function spinWords(words) {
  return words
    .split(' ')
    .map(function (word) {
      return word.length > 4 ? word.split('').reverse().join('') : word;
    })
    .join(' ');
}

console.log(spinWords('hey fellow warriors'));  // 'hey wollef sriorraw'  
